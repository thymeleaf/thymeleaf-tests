%TEMPLATE_MODE HTML
# ------------------------------------------------------------
%CONTEXT
onevar = 'one'
# ------------------------------------------------------------
%INPUT
<span th:each="name : ${#httpServletRequest.attributeNames}" th:text="${name}">...</span>
# ------------------------------------------------------------
%OUTPUT
<span>onevar</span>
