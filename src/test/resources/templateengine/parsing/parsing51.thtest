%TEMPLATE_MODE XML
# ------------------------------------------------------------
%EXACT_MATCH true
# ------------------------------------------------------------
%CONTEXT
data = {'alpha','beta'}
# ------------------------------------------------------------
%INPUT
<div>
    <p th:each="d : ${data}" th:text="${d}">...</p>
</div>
# ------------------------------------------------------------
%OUTPUT
# This is XML mode, so the engine should not make assumptions about the 'beauty' of preceding whitespace
<div>
    <p>alpha</p><p>beta</p>
</div>
